/**
 * File:
 *   cfg_rear.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/rear
 *   using the sysconfig-agent
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/rear</tt>
 * in an easy manner.
 */
.sysconfig.rear

`ag_ini(
  `SysConfigFile("/etc/rear/local.conf")
)
